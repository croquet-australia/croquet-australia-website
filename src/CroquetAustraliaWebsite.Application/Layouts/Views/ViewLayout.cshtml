@* ViewLayout.cshtml is called by ~/App/_ViewStart.cshtml and is used by views served from ~/App/* folders. *@
@using CroquetAustraliaWebsite.Application.App.Infrastructure
@using CroquetAustraliaWebsite.Application.Layouts.Shared
@model CroquetAustraliaWebsite.Application.App.Infrastructure.IViewModel
@{
    var model = (Model ?? new ViewModel());

    SharedLayout.Render(this, model);
}
